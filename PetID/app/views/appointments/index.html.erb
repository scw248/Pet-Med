<div>
<% @appointments.each do |appointment|%>
<p><%= appointment.reason_for_visit %></p>
<br>
<p><%= appointment.date_time.strftime('%b %e, %l:%M %p') %></p>
<% end %>
</div>

<div class="buttons ">
<%# <%= link_to 'New Appointment', new_user_appointment_path(@appointment)%> %>
</div>

