<% if @appointment.errors.any? %>
  <div class="error_explanation">
    <h2>
      <%= pluralize(@appointment.errors.count, "error") %>
      prohibited this post from being saved:
    </h2>
 
    <ul>
    <% @appointment.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for @appointment,url: user_appointments_path(@appointment.user) do |f| %>
<%= f.hidden_field :user_id %>
<%= f.label :pet %>
<%# <%= f.collection_select :user_id.pets, Pet.all, :id, :name %> %>
<br>
<br>
<%= f.label :vet_provider %>
<%= f.collection_select :vet_provider_id, VetProvider.all, :id, :name %>
<br>
<br>
<%= f.label :reason_for_visit%>
<%# <%= f.collection_select :reason_for_visit, Appointment.all, :reason_for_visit, :reason_for_visit %> %>
<%= f.text_field :reason_for_visit%>
<br>
<br>
<%= f.label :date_time %>
<%= f.datetime_select :date_time %>
<br>
<br>
<%= f.submit %>
<br>
<br>
<br>
<% end %>